// Los argumentos son los valores que le pasamos a una funcion

function sumar(a, b) {
  //a es un parametro

  return a + b;
}

let resultado = sumar(5, 5); //5 es un argumento
console.log(resultado);
console.log(typeof resultado);
