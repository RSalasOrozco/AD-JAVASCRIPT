// estableceremos el orden de las operaciones a nuestro gusto
// si deseamos establecer un orden de ejecucion debemos hacerlo dentro de parentesis, dentro de los parentesis debe estar la operacion o la funciones que queremos que se ejecute primero
let resultado = 8 / (2 * (2 + 2));
console.log(resultado);
